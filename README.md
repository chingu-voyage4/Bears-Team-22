# Bears-Team-22
In this voyage we are cloning the [JobBatical](https://jobbatical.com/) web site

The project is divede in two a front-end and a back-end

To install the projects you can run 
  `yarn install-all`

and it will intall all the dependency of both project usign gulp.js

or you can install the dependency of each project by going to inside the directory of each one and doing yarn install.

